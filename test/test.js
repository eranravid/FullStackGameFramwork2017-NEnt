var expect = require("chai").expect;
var rewire = require("rewire");
var app = rewire("../src_server/index");
const bonusPossibility = 15;
describe('server app', function() {
	describe('#server initialization', function() {
		it('it should work ;)');
	});
});